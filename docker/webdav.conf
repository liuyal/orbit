DavLockDB /usr/local/apache2/var/DavLock

<Location /files>
    DAV On
    Options Indexes
    Require all granted

    # Allow only GET and PUT; deny all other methods (DELETE, MOVE, COPY, etc.)
    <LimitExcept GET PUT>
        Require all denied
    </LimitExcept>
</Location>

